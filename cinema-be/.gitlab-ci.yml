image: tiangolo/docker-with-compose

before_script:
  - docker login -u gitlab-ci-token -p $CI_JOB_TOKEN $CI_REGISTRY
  - pip install docker-auto-labels

stages:
  - test
  - build
  - deploy

tests:
  stage: test
  script:
    - sh ./scripts/test.sh
  tags:
    - build
    - test

build-stag:
  stage: build
  script:
    - TAG=stag FRONTEND_ENV=staging sh ./scripts/build-push.sh
  only:
    - master
  tags:
    - build
    - test
    
build-prod:
  stage: build
  script:
    - TAG=prod FRONTEND_ENV=production sh ./scripts/build-push.sh
  only:
    - production
  tags:
    - build
    - test

deploy-stag:
  stage: deploy
  script: 
    - >
      DOMAIN=stag.localhost:8080
      TRAEFIK_TAG=stag.localhost:8080
      STACK_NAME=stag-localhost:8080
      TAG=stag
      sh ./scripts/deploy.sh
  environment:
    name: staging
    url: https://stag.localhost:8080
  only:
    - master
  tags:
    - swarm
    - stag

deploy-prod:
  stage: deploy
  script:
    - >
      DOMAIN=localhost:8080
      TRAEFIK_TAG=localhost:8080
      STACK_NAME=localhost:8080
      TAG=prod
      sh ./scripts/deploy.sh
  environment:
    name: production
    url: https://localhost:8080
  only:
    - production
  tags:
    - swarm
    - prod
